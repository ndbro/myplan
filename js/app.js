let options = {
  strings: ['My Plan', 'One step to future'],
  typeSpeed: 60,
  startDelay:500,
  backSpeed: 50,
  backDelay: 500,
  loop: true,
  loopCount: Infinity
};

let typed = new Typed('.element', options);